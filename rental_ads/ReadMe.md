# Анализ объявлений об аренде жилья с использованием LLM

Этот проект направлен на извлечение структурированной информации — **количества людей, желающих снять жильё** — из неструктурированных текстов объявлений об аренде. Данные содержат свободный текст, в котором информация о количестве проживающих указана неявно (например, «ищем квартиру на 4 человек» или «двое взрослых и ребёнок»). Для автоматического извлечения этой информации используется **локальная языковая модель (LLM)**.

---

## 🛠️ Используемые технологии

- **Python** — основной язык программирования
- **Pandas** — для работы с данными
- **LangChain + Ollama** — для взаимодействия с локальной LLM
- **Gemma 3 (12B)** — локальная модель от Google, запускается через Ollama
- **Docker** — для контейнеризации и запуска модели

---

## 📁 Структура проекта
```commandline
rental_ads/ 
├── data/ 
│ ├── submission100lines.csv # Исходный датасет с объявлениями 
│ └── llm_counts_people.csv # Результат работы модели (с предсказанным числом людей) 
├── docker-compose.yml # Конфиг для запуска Ollama в Docker 
└── peoples_from_rental_ads.ipynb # Jupyter-ноутбук с полным кодом анализа
```

### Пример промта для извлечения
```text
Ответь на вопрос, опираясь на контекст ниже.
Если на вопрос нельзя ответить, используя информацию из контекста,
ответь '0'. В контексте предоставлены заявки на аренду жилья, в которых упоминается количество человек, желающих снять жилье.

Context: {text_input}

Questions: Сколько человек планируют проживать в жилье в заявке, включая взрослых и детей?

Answer: Верни только одно число без какого-либо дополнительного текста.
```

## 🎯 Возможности и цели

- Извлечение числовых данных из естественного языка
- Оценка точности LLM на реальных данных
- Подготовка данных для дальнейшего анализа: статистика по количеству жильцов, сезонность, предпочтения и т.д.

---

## 📌 Примечание

- Модель работает локально — **не требуется интернет или API-ключи**.
- Можно легко заменить модель (например, на `llama3`, `mistral` и др.) в параметре `model` у `ChatOllama`.
- Проект легко масштабируется на большие датасеты с использованием пакетной обработки или асинхронных вызовов.

---

## 🧠 Будущие улучшения

- Добавление извлечения срока аренды
- Распознавание количества детей и взрослых отдельно
- Построение статистики и визуализаций

💡 **Цель проекта** — показать, как с помощью локальных LLM можно эффективно извлекать информацию из неструктурированных текстов без передачи данных в облако.